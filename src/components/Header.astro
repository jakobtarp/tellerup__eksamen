<!-- ---
import Button from "./Button.astro";
const { hasSearch } = Astro.props;
---

<header class="flex p-3">
  <header class="logo flex mx-auto cursor-pointer items-center">
    <img class="h-10 mr-10 my-auto" src="imgs/nextleft.png" alt="arrow" />
    <img class="h-10 my-auto" src="imgs/nextright.png" alt="arrow" />

    {hasSearch && <input class="text-gray-800 text-center rounded-full px-8 text-xs py-4" type="text" placeholder="Hvad vil du høre?" />}
  </header>

  <div class="ml-auto mt-5 mr-5 text-lg">
    <Button variant="secondary" link="/courses">Sign Up</Button>
    <Button variant="primary" link="/courses">Log in</Button>
  </div>
</header> --><!-- 
<style>
  header {
    display: flex;
    gap: 1rem;
    justify-content: space-between;
    padding-block: 1rem;
  }

  a {
    font-weight: 800;
    text-decoration: none;
    font-size: 1.5rem;
  }
</style> -->
